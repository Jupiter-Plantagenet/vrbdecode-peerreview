# eval/archive

This directory holds **archived (old) evaluation outputs** after major refactors or protocol changes.

- The repo ignores `eval/archive/*` by default to avoid committing large, stale data.
- Add a subfolder here only when you intentionally archive a previous run (e.g. before a full rerun).

